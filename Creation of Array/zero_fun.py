# for default values in  future if we have to handle with big data then
# we can fill the zero with numbers 
#  and in the python there is buitin fun for it 

import numpy as np
zeroes_array = np.zeros([3])
print(zeroes_array)